// Gemini AI functionality removed. Keep a small stub to avoid breaking imports if any remain.
export async function generateDescriptionForProduct(productName: string): Promise<string> {
  // Return an empty description; feature intentionally disabled.
  return '';
}
